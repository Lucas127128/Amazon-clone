{
  "name": "amazon-clone",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "temporal-polyfill-lite": "^0.3.3",
      "@preact/signals-core": "^1.14.1",
      "valibot": "^1.3.1",
      "vite": "^8.0.7"
    }
  },
  "type": "module",
  "scripts": {
    "lint": "bunx -b oxlint -c ./oxlint.config.ts",
    "fmt": "oxfmt",
    "dev": "bun run --parallel --elide-lines 10000 -F '*' dev",
    "test": "bun run --cwd ./packages/tests test",
    "start": "bun run --cwd ./packages/server start",
    "build": "bun run --cwd ./packages/web build"
  },
  "devDependencies": {
    "@preact/eslint-plugin-signals": "^0.2.0",
    "@socketsecurity/bun-security-scanner": "^1.1.2",
    "@types/bun": "^1.3.11",
    "@types/trusted-types": "^2.0.7",
    "eslint-plugin-depend": "^1.5.0",
    "eslint-plugin-simple-import-sort": "^13.0.0",
    "husky": "^9.1.7",
    "knip": "^6.3.0",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "oxlint-tsgolint": "^0.20.0",
    "vite": "catalog:"
  }
}
