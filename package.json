{
  "dependencies": {
    "bun-storage": "^2.0.5",
    "dayjs": "^1.11.19",
    "happy-dom": "^20.0.11",
    "vite": "^7.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "@vitest/browser-preview": "^4.0.16",
    "vitest": "^4.0.16"
  },
  "scripts": {
    "test:data": "vitest --config=vitest.browser.config.js --browser=safari tests/data",
    "test:checkout": "vitest --config=vitest.browser.config.js --browser=safari tests/checkout",
    "test:orders-api": "vitest --config=vitest.browser.config.js --browser=safari tests/api/orders-api.test.ts",
    "test:products-api": "vitest  --config=vitest.browser.config.js --browser=safari tests/api/products-api.test.ts",
    "test:api": "vitest --config=vitest.browser.config.js --browser=safari tests/api",
    "test:money": "vitest tests/utils/money.test.ts",
    "build": "vite build"
  }
}
