{
  "dependencies": {
    "bun-storage": "^2.0.5",
    "dayjs": "^1.11.19",
    "happy-dom": "^20.0.11",
    "vite": "^7.3.0"
  },
  "devDependencies": {
    "@vitest/browser-preview": "^4.0.16",
    "vitest": "^4.0.16"
  },
  "scripts": {
    "test:data": "vitest tests/data",
    "test:checkout": "vitest --config=vitest.browser.config.js --browser=safari tests/checkout",
    "test:orders": "vitest --config=vitest.browser.config.js --browser=safari tests/api/orders-api.test.js",
    "test:products": "vitest  --config=vitest.browser.config.js --browser=safari tests/api/products-api.test.js",
    "build": "vite build"
  }
}
